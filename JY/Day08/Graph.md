## 📚 다양한 그래프 알고리즘

- 그래프란 노드와 노드 사이에 연결된 간선의 정보를 가지고 있는 자료구조이다.
- 알고리즘 문제를 접했을 때, `서로 다른 개체(혹은 객체)가 연결되어 있다`는 내용이 나오면 그래프 사용을 염두해야 한다.
- 그래프 자료구조 중, `트리` 자료구조는 부모에서 자식으로 내려오는 계층적인 모델에 속하며, 컴퓨터공학 분야에서는 보통 방향 그래프로 간주된다.

  | 구분                | 그래프(Graph)                  | 트리(Tree)     |
  | ------------------- | ------------------------------ | -------------- |
  | 방향성              | 방향 그래프 또는 무방향 그래프 | 방향 그래프    |
  | 순환성              | 순환 및 비순환 가능            | 비순환         |
  | 루트 노드 존재 여부 | 루트 노드 없음                 | 루트 노드 존재 |
  | 노드 간 관계성      | 부모-자식 관계 없음            | 부모-자식 관계 |
  | 모델의 종류         | 네트워크 모델                  | 계층 모델      |

- 그래프의 구현 방법
  - (1) 인접 행렬: 2차원 배열을 이용하는 방식
  - (2) 인접 리스트: 리스트를 사용하는 방식

### 1. 서로소 집합(Disjoint Sets) 자료구조

- 서로소 집합이란 공통 원소가 없는 두 집합을 의미한다.
- `union-find` 자료구조라고 불리기도 한다.
- 서로소 집합 자료구조롤 구현할 때는 트리 자료구조를 이용하여 집합을 표현한다. union 연산은 그래프에서의 간선으로 표현된다.
- `서로소 집합 계산 알고리즘`
  1. union 연산을 확인하여, 서로 연결된 두 노드 A, B를 확인한다.
     - A와 B의 `루트 노드` A', B'을 각각 찾는다.
     - A'를 B'의 부모 노드로 설정한다.
  2. 모든 union 연산을 처리할 때까지 1번 과정을 반복한다.
- `유의할 점`
  1. union 연산을 효율적으로 수행하기 위해 `부모 테이블`을 항상 가지고 있어야 한다.
  2. 루트 노드를 즉시 계산하지 못하므로, 부모 테이블을 계속 확인하며 거슬러 올라가야 한다.
- `무방향` 그래프 내에서의 사이클 판별에 사용 가능하다.
  1. 각 간선을 확인하며 두 노드의 루트 노드를 확인한다.
     - 루트 노드가 서로 다르다면 두 노드에 대하여 union 연산을 수행한다.
     - 루트 노드가 서로 같다면 사이클이 발생할 것이다.
  2. 그래프에 포함되어 있는 모든 간선에 대하여 1번 과정을 반복한다
